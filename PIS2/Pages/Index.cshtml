@page
@model IndexModel
@{
    ViewData["Title"] = "Welcome";
    var successMessage = TempData["SuccessMessage"] as string;
  
}
@Html.AntiForgeryToken();

<!--PAGE TITLE-->
<div class="bg-light border rounded p-4 mb-4 d-print-none">
    <div class="row align-items-center">
        <div class="col-md-8">
            <h1 class="fw-bold mb-1">Employee Self-Service Portal</h1>
            <p class="text-muted mb-0">
                Access your employment records, leave, overtime, and company services in one place.
            </p>
        </div>
        <div class="col-md-4 text-md-end mt-3 mt-md-0">
            @if (Model.isSelf)
            {
                <a asp-page="/Person/Details" asp-route-id="@Model.Person.personID">
                    <span class="badge bg-primary fs-6 px-3 py-2">
                        My Records
                    </span>
                </a>  
            }
            
        </div>
    </div>
</div>

<!--MESSAGE FROM HANDLER-->
@if (!string.IsNullOrEmpty(successMessage))
{
    <div class="alert alert-success d-print-none" role="alert">
        @successMessage
    </div>
}
<!--MAIN BODY-->
<div class="row mt-1 d-flex justify-content-center">
    <!--PERSON DETAILS SECTION-->
    <div class="col-md-3 d-print-none">
        <div class="card shadow-sm border-0 mb-3 position-sticky" style="top: 1rem;">
            <div class="card-header bg-primary text-white text-center py-2">
                <h5 class="fw-bold mb-0">Employee Lookup</h5>
            </div>
            <div class="card-body">
                <!-- Search Controls -->
                <h6 class="fw-semibold mb-2 text-secondary"><i class="bi bi-search me-1"></i>Find Employee</h6>
                <hr class="mt-0" />

                <!-- Search by Employee ID -->
                <form method="post" asp-page-handler="SearchID" class="input-group mb-3">
                    <input id="givenID" name="searchID" type="text" class="form-control" placeholder="Search by Employee ID"
                           asp-for="@Model.Employment.givenID" list="givenIDList">
                    <datalist id="givenIDList">
                        @foreach (var item in Model.Employments)
                        {
                            <option value="@item.givenID"></option>
                        }
                    </datalist>
                    <button type="submit" class="btn btn-primary">View</button>
                </form>

                <!-- Search by Name -->
                <form method="post" asp-page-handler="SearchName" class="input-group mb-3">
                    <input id="employeeName" name="searchName" type="text" class="form-control" placeholder="Search by Name"
                           asp-for="@Model.Person.personFullName" list="namesList">
                    <datalist id="namesList">
                        @foreach (var item in Model.People)
                        {
                            <option value="@item.personFullName">@item.personFullName</option>
                        }
                    </datalist>
                    <button type="submit" class="btn btn-primary">View</button>
                </form>

                <!-- Person Details -->
                @if (Model.Person != null && Model.Person?.personID != 0)
                {
                    <div class="card mt-3 border-0 shadow-sm">
                        <div class="card-body text-center p-3">

                            <img src="/images/@(Model.Person?.personID).jpg" class="rounded-circle border shadow-sm" style="width: 120px; height: 120px; object-fit: cover;" alt="Profile Picture"
                                 onerror="this.onerror=null; this.src='/images/@(Model.Person.personGender == Models.Gender.Female ? "female-default.png" : "male-default.png")';">

                            <h6 class="fw-bold mb-1 text-primary">
                                @if (Model.Employment != null)
                                {
                                    if (User.IsInRole("MIE\\PMS_HRCLERK"))
                                    {
                                        <span onclick="location.href='@Url.Page("/Employment/Edit", new { id = Model.Employment.employmentID })'"
                                              style="cursor:pointer; text-decoration:underline;">
                                            @Html.DisplayFor(model => model.Person.personFullName)
                                        </span>
                                    }
                                    else
                                    {
                                        <span onclick="location.href='@Url.Page("/Employment/Details", new { id = Model.Employment.employmentID })'"
                                              style="cursor:pointer; text-decoration:underline;">
                                            @Html.DisplayFor(model => model.Person.personFullName)
                                        </span>
                                    }
                                }
                                else
                                {
                                    <span onclick="location.href='@Url.Page("/Person/Details", new { id = Model.Person.personID })'"
                                          style="cursor:pointer; text-decoration:underline;">
                                        @Html.DisplayFor(model => model.Person.personFullName)
                                    </span>
                                }
                            </h6>

                            <p class="mb-1 small text-muted">
                                <strong>Gender:</strong> @Html.DisplayFor(m => m.Person.personGender)
                            </p>
                            <p class="mb-1 small text-muted">
                                <strong>Phone:</strong> @Html.DisplayFor(m => m.Person.personPhoneNumber)
                            </p>
                            <p class="mb-1 small text-muted">
                                <strong>Address:</strong> @Html.DisplayFor(m => m.Person.addressModel.addressFormatted)
                            </p>
                            <p class="mb-2 small text-muted">
                                <strong>Email:</strong> @Html.DisplayFor(m => m.Person.personEmailAddress)
                            </p>

                            @if (Model.PersonEmployments != null)
                            {
                                <hr class="my-2" />
                                <p class="mb-1 small text-muted">
                                    <strong>Job Title:</strong> @Html.DisplayFor(m => m.JobPlacement.jobModel.jobTitle)
                                </p>
                                <p class="mb-1 small text-muted">
                                    <strong>Department:</strong> @Html.DisplayFor(m => m.JobPlacement.departmentModel.departmentName)
                                </p>
                            }

                            <a class="d-block mt-2 small fw-semibold text-decoration-underline text-primary"
                               onclick="location.href='@Url.Page("/Person/Details", new { id = Model.Person.personID })'"
                               style="cursor:pointer;">
                                View Full Details
                            </a>
                        </div>
                    </div>
                }
                else
                {
                    <div class="alert alert-light border mt-3 text-center py-3">
                        <p class="mb-0 text-muted">No employee selected.</p>
                    </div>
                }
            
            </div>
        </div>
    </div>

<!-- right side column -->
    
    <div class="col-md-9 d-print-flex print-full-width">
        @if (Model.Person != null && Model.PersonEmployments != null && Model.Employment != null)
        {
            <!--TOP BUTTONS -->
            <div class="card shadow-sm border-0 mb-3 d-print-none">
                <div class="card-body p-0">
                    <div class="d-flex flex-wrap">

                        @if(Model.Employment.employmentStatus == Models.mainStatus.Active)
                        {
                            <!--Leave Request button-->
                            @if (Model.IsLeaveAllow)
                            {
                                <a role="button" class="btn btn-outline-primary flex-fill rounded-0 border-0" asp-page="/Leave/Create" asp-route-id="@Model.Employment.employmentID">Leave Request</a>
                            }

                            <!--Ovetime Button-->
                            @if (Model.IsOvertimeAllow)
                            {
                                <a role="button" class="btn btn-outline-primary flex-fill rounded-0 border-0" asp-page="/OvertimeRecord/Create" asp-route-id="@Model.Employment.employmentID">Overtime Record</a>
                            }
                    
                        }
                
                        <!--leave report-->
                        @if(Model.Leaves!=null && Model.Leaves.Any() && Model.isSelf)
                        {                  
                            <a role="button" class="btn btn-outline-primary flex-fill rounded-0 border-0" id="leaveHistory" data-bs-toggle="collapse" data-bs-target="#leaveHistoryView" onclick="closeOtherCollapses('#collapseOne')">Leave History</a>
                            <a role="button" class="btn btn-outline-primary flex-fill rounded-0 border-0" id="leaveAggrigate" data-bs-toggle="collapse" data-bs-target="#leaveAggrigate" onclick="closeOtherCollapses('#collapseOne')">Leave Summary</a>
                            <a role="button" class="btn btn-outline-primary flex-fill rounded-0 border-0" id="overtimeHistory" data-bs-toggle="collapse" data-bs-target="#overtimeHistoryView" onclick="closeOtherCollapses('#collapseOne')">Overtime History</a>
                            <a role="button" class="btn btn-outline-primary flex-fill rounded-0 border-0" id="adjustment" data-bs-toggle="collapse" data-bs-target="#employmentHistoryView" onclick="closeOtherCollapses('#employmentHistoryView')">Employment History</a>
                        }
                     </div>
                </div>
            </div>
            <!--ROW FOR VIEWS-->
            <div class="row mt-1 d-flex justify-content-center">
                <!--LEAVE SUMMARY-->
                <div class="row collapse border-primary rounded @(TempData["KeepLeaveAggrigate"] != null ? "show" : "")" id="leaveAggrigate">

                   @await Html.PartialAsync("Employment/_LeaveSummaryPartial", Model.LeaveDetail)           
                </div>     
                <!--OVERTIME HISTORY-->
                <div class="col-md-12 collapse border-primary rounded @(TempData["KeepOvetimeHistory"] != null ? "show" : "")" id="overtimeHistoryView">

                    @if (Model.Employment == null)
                    {
                        <p>@Model.Person.personFullName is not an emplyee.</p>
                    }
                    else
                    {
                        @await Html.PartialAsync("Employment/_OvertimeHistoryPartial", Model.Overtimes)
                    }

                </div>
                <!--LEAVE HISTORY-->
                <div class="col-md-12 collapse border-primary rounded @(TempData["KeepLeaveHistory"] != null ? "show" : "")" id="leaveHistoryView">

                    @if (Model.Employment == null)
                    {
                        <p>@Model.Person.personFullName is not an active employee.</p>
                    }
                    else
                    {
                        @await Html.PartialAsync("Employment/_LeaveHistoryPartial", Model.Leaves)
                    }
                        
                </div>
                <!--EMPLOYMENT HISTORY -->
                <div class="col-md-12 collapse border-primary rounded @(TempData["KeepCollapseOpen"] != null ? "show" : "")" id="employmentHistoryView">
                    <div class="card shadow-sm border-0 mb-4">
                        <div class="card-header d-flex justify-content-around bg-white border-bottom">
                            <h3 class="fw-bold col-md-6">Employment History</h3>
                            @if (Model.isSelf && Model.PersonEmployments.FirstOrDefault(pe => pe.employmentStatus == Models.mainStatus.Active) != null)
                            {
                                <div class="d-flex p-2">
                                    <button type="button" class="form-control btn btn-primary m-2 d-print-none" id="btnExperience" data-request-type="Exprience" onclick="requestService(this)">
                                        Request Official Experience
                                    </button>

                                   
                                    <a class="form-control btn btn-primary m-2 d-print-none" id="btnTermination" asp-page="/Termination/Create"
                                       asp-route-id="@Model.PersonEmployments.FirstOrDefault(pe => pe.employmentStatus == Models.mainStatus.Active).employmentID">Request Termination</a>
                                    @if (Model.IsGuarantyAllow)
                                    {
                                        <a href="/Guaranty/Create" class="form-control btn btn-primary m-2 d-print-none" id="btnGuarantee">Request Guaranty</a>
                                    }
                                </div>
                            }
                        
                        </div>
                        <div class="card-body">
                            @if (Model.PersonEmployments != null)
                            {
                                <table class="table table-sm table-striped table-hover">
                                    <thead>
                                        <tr class="bg-dark text-white">
                                            <th>
                                                Given ID
                                            </th>
                                            <th>
                                                Full Name
                                            </th>
                                            <th>
                                                Emplyment Date
                                            </th>
                                            <th>
                                                Employment Status
                                            </th>
                                            <th>
                                                Employment Reference
                                            </th>
                                    
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model.PersonEmployments)
                                        {
                                            <tr onclick="location.href='@Url.Page("/Employment/Details", new {id = item.employmentID})'" style="cursor:pointer">
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.givenID)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.personModel.personFullName)
                                                </td>
                                                <td>
                                                    @item.employmentDate.ToString("MMM dd, yyyy")
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.employmentStatus)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.employmentReference)
                                                </td>

                                            </tr>
                                            <h4 class="fw-bold">Job Placements</h4>

                                            <table class="table table-striped table-hover">
                                            
                                                <thead>
                                                    <tr class="bg-dark text-white">                                                    
                                                        <th>
                                                            Department
                                                        </th>
                                                        <th>
                                                            Job Title
                                                        </th>
                                                    
                                                        <th>
                                                            Salary
                                                        </th>
                                                        <th>
                                                            Date
                                                        </th>
                                                        <th>
                                                            Grade
                                                        </th>
                                                        <th>
                                                            Step
                                                        </th>
                                                        <th>
                                                            Status
                                                        </th>
                                                    
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var jp in item.JobPlacements.OrderBy(j => j.jobPlacementDate))
                                                    {
                                                        <tr onclick="location.href='@Url.Page("/JobPlacement/Details", new {id = jp.jobPlacementID})'" style="cursor:pointer">
                                                        
                                                            <td>
                                                                @Html.DisplayFor(modelItem => jp.departmentModel.departmentName)
                                                            </td>
                                                            <td>
                                                                @Html.DisplayFor(modelItem => jp.jobModel.jobTitle)
                                                            </td>
                                                        
                                                            <td>
                                                                @Html.DisplayFor(modelItem => jp.jobPlacementSalary)
                                                            </td>
                                                            <td>
                                                                @jp.jobPlacementDate.ToString("MMM dd, yyyy")
                                                            </td>
                                                            <td>
                                                                @jp.jobModel?.jobGradeModel?.jobGradeName
                                                            </td>
                                                            <td>
                                                                @jp.jobStepModel?.jobStepName
                                                            </td>
                                                            <td>
                                                                @Html.DisplayFor(modelItem => jp.jobPlacementStatus)
                                                            </td>
                                                        
                                                        </tr>
                                                    }
                                                </tbody>
                                            </table>
                                        }
                                    </tbody>
                                </table>
                            }
                            else
                            {
                                <p>No employment records.</p>
                            }

                        </div>
                        <div class="card-footer">
                            @await Html.PartialAsync("print")
                        </div>
                    </div>
                </div>
            </div>
        }
        
        <!--COMPANY HUB-->
        <div class="card shadow-sm border-0 mb-4">
            <!--NOTICE BOARD-->
            <div class="card-header bg-warning-subtle">
                <h5 class="fw-bold mb-0">
                    <i class="bi bi-megaphone-fill text-warning me-2"></i> <span class="text-warning"> Notice Board</span>
                </h5>
            </div>
            <div class="card-header bg-warning-subtle">
                <partial name="_NoticeCarousel" model="Model.ActiveNoticesForCarousel" />
            </div>
        </div>        
            <!--FORMS-->
            <style>
                /* CSS for the professional hover effect */
                .card-hover {
                    transition: transform 0.2s, box-shadow 0.2s;
                }

                    .card-hover:hover {
                        transform: translateY(-5px); /* Lift card slightly */
                        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important; /* Enhanced shadow on hover */
                    }
            </style>
        <div class="card shadow-sm border-0 mb-4">

            <div class="card-header p-4 border rounded shadow-sm bg-white d-print-none">
                <h2 class="fw-bold mb-3 text-center text-dark"><i class="bi bi-file-earmark-text fs-3 text-primary mb-2"></i>Company Forms & Requests</h2>
                <p class="text-center text-muted mb-5">Select a form below to initiate a request and fill forms.</p>
            </div>
            <div class="card-body">
                <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Request authorization to move goods from a site.</p>
                                <h5 class="fw-bold text-primary mb-0">
                                    <a href="@Url.Page("/Forms/GoodsGatePass")" class="text-primary text-decoration-underline">Goods Gate Pass</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Form for transferring fixed assets between locations.</p>
                                <h5 class="fw-bold text-success mb-0">
                                    <a href="@Url.Page("/Forms/GoodsTransfer")" class="text-success text-decoration-underline">Goods Transfer</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Request Windows, Internet, or Company Email access.</p>
                                <h5 class="fw-bold text-danger mb-0">
                                    <a href="@Url.Page("/Forms/GeneralUserAccess")" class="text-danger text-decoration-underline">General User Access</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Request login credentials for the PIS system.</p>
                                <h5 class="fw-bold text-primary mb-0">
                                    <a href="@Url.Page("/Forms/PISAccess")" class="text-primary text-decoration-underline">PIS Access</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Specific access request form for the SL system.</p>
                                <h5 class="fw-bold text-success mb-0">
                                    <a href="@Url.Page("/Forms/SLAccess")" class="text-success text-decoration-underline">SL Access</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Template and format for official interoffice correspondence.</p>
                                <h5 class="fw-bold text-danger mb-0">
                                    <a href="@Url.Page("/Forms/InterOffice")" class="text-danger text-decoration-underline">Interoffice Memo</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Submit a formal report for sick leave or absence.</p>
                                <h5 class="fw-bold text-info mb-0">
                                    <a href="@Url.Page("/Forms/SickReport")" class="text-info text-decoration-underline">Sick Report</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card text-center border-0 shadow-sm h-100 card-hover">
                            <div class="card-body p-4">
                                <p class="text-secondary small mb-2">Transport request form</p>
                                <h5 class="fw-bold text-info mb-0">
                                    <a href="@Url.Page("/Forms/Transport")" class="text-info text-decoration-underline">Transport Request</a>
                                </h5>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    
</div>




<script>

    function closeOtherCollapses(targetId) {
        const collapsibles = document.querySelectorAll('.collapse');
        collapsibles.forEach(collapse => {
            if (collapse.id !== targetId.substring(1)) {
                const bsCollapse = bootstrap.Collapse.getInstance(collapse);
                if (bsCollapse) bsCollapse.hide();
            }
        });
    }


    let requestInProgress = false;

    async function requestService(button) {
        
        const requestType = button.dataset.requestType;
        const tokenInput = document.querySelector('input[name="__RequestVerificationToken"]');
        const token = tokenInput ? tokenInput.value : null;

        try {
            const response = await fetch("/Index?handler=HandleRequest", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                    "RequestVerificationToken": token
                },
                body: JSON.stringify({ requestType })
            });

            const result = await response.json();

            if (result.success)
            {
                alert("Your request has been submitted successfully.");
            } else {
                alert(result.message || "Request failed. Please try again.");
            }
        } catch (error) {

            console.error(error);
            alert("An unexpected error occurred. Please try again later.");
            
        }
    }


</script>








