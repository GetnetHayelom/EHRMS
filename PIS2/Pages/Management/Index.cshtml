@page
@model PIS2.Pages.Management.IndexModel
@{
    ViewData["Title"] = "Welcome";
    var successMessage = TempData["SuccessMessage"] as string ?? string.Empty;
}
<h1 class="d-flex justify-content-center">
    <b class="d-print-none">Management Dashboard</b>
    <b class="d-none d-print-block">Management Report</b>
</h1>

<div class="d-flex w-100">
    <div class="col-2 bg-dark text-white d-none d-print-none">
        <div>
        <img />
        </div>
        <div class="d-flex flex-column mt-6">
            <a role="button" class="text-white nav-link p-3" asp-page="/Company/Index" asp-asp-area="Management">Companies</a>
            <a role="button" class="text-white nav-link p-3" asp-page="/Department/Index" asp-asp-area="Management">Departments</a>
            <a role="button" class="text-white nav-link p-3" asp-page="/Employment/Index" asp-asp-area="Management">Employments</a>
            <a role="button" class="text-white nav-link p-3" asp-page="/Job/Index" asp-asp-area="Management">Jobs</a>
            <a role="button" class="text-white nav-link p-3" asp-page="/Worksite/Index" asp-asp-area="Management">Work Loactions</a>
        </div>
    </div>
    <div class="col d-flex flex-column">
    <!-- Summary Points -->
        <div class="row d-flex flex-inline justify-content-center">
            <div class="p-2 border borber-1 rounded-2 border-primary col-3 m-2">
                <div class="w-100">
                    <div class="d-flex">
                        <h5 class="flex-grow-1 border border-success border-2 rounded-start text-success p-1">Active Companies</h5>
                        <h2 class="bg-success text-white rounded-end p-1">@Model.ActiveCompanies</h2>
                    </div>
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-danger border-2 rounded-start text-danger p-1">Old Companies</h6>
                        <h4 class="bg-danger text-white rounded-end p-1">@Model.OldCompanies</h4>
                    </div>
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-info border-2 rounded-start text-info p-1">All Companies</h6>
                        <h4 class="bg-info text-white rounded-end p-1">@Model.Companies.Count()</h4>
                    </div>
                </div>
            </div>
            <div class="p-2 border borber-1 rounded-2 border-primary col-3 m-2">
                <div class="w-100">
            
                    <div class="d-flex">
                        <h5 class="flex-grow-1 border border-success border-2 rounded-start text-success p-1">Active Departments</h5>
                        <h2 class="bg-success text-white rounded-end p-1">@Model.ActiveDepartments</h2>
                    </div>
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-danger border-2 rounded-start text-danger p-1">Old Departments</h6>
                        <h4 class="bg-danger text-white rounded-end p-1">@Model.OldDepartments</h4>
                    </div>
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-info border-2 rounded-start text-info p-1">All Departments</h6>
                        <h4 class="bg-info text-white rounded-end p-1">@Model.Departments.Count()</h4>
                    </div>
                </div>
            </div>
            <div class="p-2 border borber-1 rounded-2 border-primary col-3 m-2">
                <div class="w-100">
                    <div class="d-flex">
                        <h5 class="flex-grow-1 border border-success border-2 rounded-start text-success p-1">Active Employees</h5>
                        <h2 class="bg-success text-white rounded-end p-1">@Model.ActiveEmployments</h2>
                    </div>
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-danger border-2 rounded-start text-danger p-1">Former Employees</h6>
                        <h4 class="bg-danger text-white rounded-end p-1">@Model.exEmployments</h4>
                    </div>
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-info border-2 rounded-start text-info p-1">Total Number of Employee</h6>
                        <h4 class="bg-info text-white rounded-end p-1">@Model.Employments.Count()</h4>
                    </div>
                </div>
            </div>
            <div class="p-2 border borber-1 rounded-2 border-primary col-2 m-2">
                <div class="w-100">
                    @foreach (var item in Model.EmploymentTypes)
                    {
                        <div class="d-flex">
                            <h5 class="flex-grow-1 border border-success border-2 rounded-start text-success p-1">@item.zName</h5>
                            <h2 class="bg-success text-white rounded-end p-1">@item.zCount</h2>
                        </div>
                
                    }
                    <div class="d-flex">
                        <h6 class="flex-grow-1 border border-info border-2 rounded-start text-info p-1">Employees On Leave</h6>
                        <h4 class="bg-info text-white rounded-end p-1">@Model.leaveEmployments</h4>
                    </div>
                </div>
            </div>
        </div>
        <!--Payables summary-->
       <h3 class="p-2">Current Payable</h3><hr />
       <div class="d-flex justify-content-between p-2">
       
            <div class="">
                <p class="small mb-1">Salary</p>
                <h5 class="fw-bold">$@Math.Round((Model.CompanySummaries.Sum(c => c.Salary)), 2).ToString("N0")</h5>
            </div>
            <div>
                <p class="small mb-1">Overtime</p>
                <h5 class="fw-bold">$@Math.Round((Model.CompanySummaries.Sum(c => c.Salary)), 2).ToString("N0")</h5>  
            </div>
            <div>
                <p class="small mb-1">Acc. Leaves</p>
                <h5 class="fw-bold">$@Math.Round((Model.CompanySummaries.Sum(c => c.Leaves.leaveCost)), 2).ToString("N0")</h5>
            </div>
            <div>
                <p class="small mb-1">Allowance</p>
                <h5 class="fw-bold">$@Math.Round((Model.CompanySummaries.Sum(c => c.Salary)), 2).ToString("N0")</h5>
            </div>
            <div>
                <p class="small mb-1">Total</p>
                <h4 class="fw-bold">$@Math.Round((Model.CompanySummaries.Sum(c => c.Salary)), 2).ToString("N0")</h4>
            </div>

       </div>
       <hr />
        <!-- Charts Container1-->
        <div class="container">
            <h3>Metrics</h3>
            <div class="row">
                <!-- Education Levels -->
                <div class="col p-2 border-start border-end border-1 border-dark">
                    <canvas id="educationLevelPieChart" height="400">
                    </canvas>
                </div>
                <!-- Work Location -->
                    <div class="col p-2 border-end border-1 border-dark">
                    <canvas id="workLocChart" height="400">
                    </canvas>
                </div>
                <!-- Job Category -->
                    <div class="col p-2 border-end borber-1 border-dark">
                    <canvas id="jobCategoryChart"  height="400">
                    </canvas>
                </div>
     
            </div>
        </div>
        <!-- Charts Container1-->

        <!--Charts Container2 -->
        <div class="row d-flex  justify-content-center">
            <!-- Employment Trends --> 
            <div class="p-2 m-2">
                <canvas id="employmentHireChart" class="w-100">
                </canvas>
            </div>
        </div>
        <!--COMPANY CARDS-->
        <div class="container py-5">
            <a class="text-center mb-4">
                <h2><b asp-page="./Company/Index" asp-area="Management" role="button">Companies</b></h2>
            </a>
            <div class="row g-4">
                @foreach (var item in Model.CompanySummaries)
                {
                    
                    <div class="col-md-3 col-lg-3">
                        <div class="card border borber-1 rounded-2 border-primary h-100">
                            <div class="card-body p-4">
                                <div class="d-flex justify-content-between align-items-start">
                                    <a asp-page="Companyp" asp-route-id="@item.CompanyID" asp-area="Management" role="button" class="btn align-items-start">
                                        <h5 class="card-title fw-bold">@item.CompanyName</h5>
                                        <h6 class="card-subtitle mb-3 opacity-75">Manager: @(item.Manager ?? "N\\A")</h6>
                                    </a>

                                </div>
                                <div class="d-flex justify-content-between border-top border-opacity-25 pt-3 text-info">
                                    <div>
                                        <p class="small mb-1">Employees</p>
                                        <h3 class="fw-bold text-info">@item.Employees.ToString("N0")</h3>
                                    </div>
                                    <div>
                                        <p class="small mb-1">Departments</p>
                                        <h3 class="fw-bold">@item.Departments</h3>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-between border-top border-opacity-25 pt-3">
                                    <div class="text-warning ">
                                        <p class="small mb-1">Acc. Leaves</p>
                                        <h5 class="fw-bold">@Math.Round(item.Leaves.AllowedLeave, 2).ToString("N0")</h5>
                                    </div>
                                    <div class="text-danger">
                                        <p class="small mb-1">Leave Cost</p>
                                        <h5 class="fw-bold ">$@Math.Round(item.Leaves.leaveCost, 2).ToString("N0")</h5>
                                    </div>
                                </div>
                                <div class="mt-4 text-success">
                                    <h4 class="display-8 fw-bold text-end">$@(item.Salary.ToString("N0"))</h4>
                                    <p class="small text-end mb-0">Total Monthly Payroll</p>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
@await Html.PartialAsync("print")
<table class="d-none">
<thead>
<tr><th>Year</th><th>Number</th></tr></thead>
<tbody>
        @foreach (var item in Model.ActiveEmployeeRate)
        {
            <tr> <td>@item.zYear</td>
                <td>@item.zCount</td>
            </tr>}
        </tbody>
    </table>
<!--EDUCATION LEVEL CHART SCRIPT-->
<script>
    var educationLevelData = @Html.Raw(Json.Serialize(Model.EduLevelSummary));
    var Total = @Model.EduLevelSummary.Select(el => el.zCount).Sum();
    var educationLevelLabels = educationLevelData.map(function (item) {
        return Math.round((item.zCount/Total)*100) + '% ' + item.zName;
    });
    var educationLevelCategories = educationLevelData.map(item => item.educationLevelCategories);
    var educationLevelCounts = educationLevelData.map(function (item) {
        return item.zCount;
    });

    var ctx = document.getElementById('educationLevelPieChart').getContext('2d');
    var educationLevelChart = new Chart(ctx, {
        type: 'doughnut',
        data: {
            labels: educationLevelLabels,
            datasets: [{
                data: educationLevelCounts,
                backgroundColor: [
                    '#FF6B6B', '#FF8E72', '#FFB74D', '#FFD54F', '#DCE775',
                    '#A3E635', '#4CAF50', '#81C784', '#64B5F6', '#2196F3',
                    '#1565C0', '#9575CD', '#BA68C8', '#F06292', '#EC407A'
                ],
                borderColor: '#fff',
                borderWidth: 2,
                hoverOffset: 15,
                hoverBorderColor: '#333'
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '50%', // This makes it a doughnut instead of pie
            plugins: {
                legend: {
                    position: 'bottom',
                    labels: {
                        padding: 10,
                        usePointStyle: true,
                        pointStyle: 'circle',
                        font: {
                            size: 12,
                            family: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif"
                        },
                        generateLabels: function (chart) {
                            const data = chart.data;
                            if (data.labels.length && data.datasets.length) {
                                return data.labels.map(function (label, i) {
                                    const meta = chart.getDatasetMeta(0);
                                    const style = meta.controller.getStyle(i);

                                    return {
                                        text: label,
                                        fillStyle: style.backgroundColor,
                                        strokeStyle: style.borderColor,
                                        lineWidth: style.borderWidth,
                                        hidden: !chart.getDataVisibility(i),
                                        index: i
                                    };
                                });
                            }
                            return [];
                        }
                    },
                    onClick: function (e, legendItem, legend) {
                        const index = legendItem.index;
                        const chart = legend.chart;
                        chart.toggleDataVisibility(index);
                        chart.update();
                    }
                },
                tooltip: {
                    callbacks: {
                        label: function (context) {
                            const label = context.label || '';
                            const value = context.raw || 0;
                            const total = context.dataset.data.reduce((a, b) => a + b, 0);
                            const percentage = Math.round((value / total) * 100);

                            return `${label}: ${value} people (${percentage}%)`;
                        }
                    },
                    displayColors: true,
                    usePointStyle: true,
                    bodyFont: {
                        size: 14
                    },
                    padding: 12
                },
                title: {
                    display: true,
                    text: "Education Level",
                    font: {
                        size: 18,
                        weight: "bold",
                        family: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif"
                    },
                    
                    padding: {
                        top: 10,
                        bottom: 30
                    },
                    align: 'center'
                }
            },
            animation: {
                animateScale: true,
                animateRotate: true
            }
        }
    });
</script>
<!--EMPLOYMENT RATES SCRIPT-->
<script>
    var ctx = document.getElementById("employmentHireChart").getContext("2d");
    // Get the serialized data from the server
    var hireData = @Html.Raw(Json.Serialize(Model.EmploymentHireRate));
    var terminationData = @Html.Raw(Json.Serialize(Model.TerminationRate));
    var manPowerRate = @Html.Raw(Json.Serialize(Model.ActiveEmployeeRate));
    
    const allYears = [...new Set([...hireData.map(item => item.zYear), ...terminationData.map(item => item.zYear)])].sort();

    // Map hire counts to years, filling missing values with 0
    const counts = allYears.map(year => {
        const found = hireData.find(item => item.zYear === year);
        return found ? found.zCount : 0;
    });

    // Map termination counts to years, filling missing values with 0
    const tcounts = allYears.map(year => {
        const found = terminationData.find(item => item.zYear === year);
        return found ? found.zCount : 0;
    });
    //Man Power Map
    const manPower = manPowerRate.map(item => item.zCount);

    
    new Chart(ctx, {
            type: 'line',
            data: {
                labels: allYears,
                datasets: [{
                    label: 'Hires',
                    data: counts,
                    backgroundColor: 'rgba(63, 81, 181, 0.2)',
                    borderColor: 'rgba(63, 81, 181, 1)',
                    borderWidth: 3,
                    tension: 0.2,
                    pointBackgroundColor: 'rgba(63, 81, 181, 1)',
                    pointRadius: 4,
                    pointHoverRadius: 6,
                    fill: true,
                    yAxisID: 'y'
                },
                {
                    label: 'Terminations',
                    data: tcounts,
                    backgroundColor: 'rgba(229, 57, 53, 0.2)',
                    borderColor: 'rgba(229, 57, 53, 1)',
                    borderWidth: 3,
                    tension: 0.2,
                    pointBackgroundColor: 'rgba(63, 81, 181, 1)',
                    pointRadius: 4,
                    pointHoverRadius: 6,
                    fill: true,
                    yAxisID: 'y'
                },
                {
                    label: 'Active Man-power',
                    data: manPower,
                    backgroundColor: 'rgba(10, 250, 53, 0.2)',
                    borderColor: 'rgba(10, 250, 53, 1)',
                    borderWidth: 3,
                    tension: 0.2,
                    pointBackgroundColor: 'rgba(63, 81, 181, 1)',
                    pointRadius: 4,
                    pointHoverRadius: 6,
                    fill: true,
                    yAxisID: 'y'
                }
                ]
            },
       
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    title: {
                        display: true,
                        text: 'Employment Trends',
                        font: {
                            size: 18,
                            weight: 'bold'
                        },
                        padding: {
                            top: 10,
                            bottom: 20
                        }
                    },
                    legend: {
                        position: 'top',
                        labels: {
                            boxWidth: 12,
                            padding: 20,
                            font: {
                                size: 12
                            }
                        }
                    },
                    tooltip: {
                        mode: 'index',
                        intersect: false,
                        callbacks: {
                            title: function (context) {
                                return 'Year: ' + context[0].label;
                            },
                            label: function (context) {
                            return context.dataset.label + context.parsed.y.toLocaleString();
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        title: {
                            display: true,
                            text: 'Year',
                            font: {
                                weight: 'bold'
                            }
                        },
                        grid: {
                            display: false
                        }
                    },
                    y: {
                        title: {
                            display: true,
                            text: 'Amount',
                            font: {
                                weight: 'bold'
                            }
                        },
                        min: Math.max(0, Math.min(...counts) - Math.max(...counts) * 0.1),
                        ticks: {
                            callback: function (value) {
                                return value.toLocaleString();
                            }
                        }
                    }
                },
                interaction: {
                    intersect: false,
                    mode: 'nearest'
                }
            }
        });
</script>
<!--PAYABLE PIE CHART SCRIPT-->
<script>
    var ctx = document.getElementById("non").getContext("2d");
    var workSiteEmployees = @Html.Raw(Json.Serialize(Model.JobCategorySummary));
    const worksite = workSiteEmployees.map(item => item.zName);
    const employees = workSiteEmployees.map(item => item.zCount);

    new Chart(ctx, {
        type: 'pie',
        data: {
            labels: worksite,
            datasets: [{
                data: employees,
                backgroundColor: [
                    '#3f51b5', '#2196f3', '#00bcd4', '#009688',
                    '#4caf50', '#8bc34a', '#cddc39', '#ffc107'
                ],
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            plugins: {
                title: {
                    display: true,
                    text: 'Employment Distribution by Work Location',
                    font: {
                        size: 18,
                        weight: 'bold'
                    },
                    padding: {
                        top: 10,
                        bottom: 20
                    }
                },
                legend: { position: 'bottom' },
                tooltip: {
                    callbacks: {
                        label: function (context) {
                            const total = context.dataset.data.reduce((a, b) => a + b);
                            const percentage = Math.round((context.raw / total) * 100);
                            return `${context.label}: ${context.raw} (${percentage}%)`;
                        }
                    }
                }
            }
        }
    });
</script>
<!--JOB CATEGORY CHART SCRIPT-->
<script>
    var jobCategoryData = @Html.Raw(Json.Serialize(Model.JobCategorySummary));
    var Total = @Model.JobCategorySummary.Select(el => el.zCount).Sum();
    var jobCategoryLabels = jobCategoryData.map(function (item) {
        return Math.round((item.zCount / Total) * 100) + '% ' + item.zName;
    });
    
    var jobCategoryCounts = jobCategoryData.map(function (item) {
        return item.zCount;
    });

    var ctx = document.getElementById('jobCategoryChart').getContext('2d');
    var jobCategoryChart = new Chart(ctx, {
        type: 'doughnut',
        data: {
            labels: jobCategoryLabels,
            datasets: [{
                data: jobCategoryCounts,
                backgroundColor: [
                    '#FF6B6B', '#FF8E72', '#FFB74D', '#FFD54F', '#DCE775',
                    '#A3E635', '#4CAF50', '#81C784', '#64B5F6', '#2196F3',
                    '#1565C0', '#9575CD', '#BA68C8', '#F06292', '#EC407A'
                ],
                borderColor: '#fff',
                borderWidth: 2,
                hoverOffset: 15,
                hoverBorderColor: '#333'
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '50%', // This makes it a doughnut instead of pie
            plugins: {
                legend: {
                    position: 'bottom',
                    labels: {
                        padding: 10,
                        usePointStyle: true,
                        pointStyle: 'circle',
                        font: {
                            size: 12,
                            family: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif"
                        },
                        generateLabels: function (chart) {
                            const data = chart.data;
                            if (data.labels.length && data.datasets.length) {
                                return data.labels.map(function (label, i) {
                                    const meta = chart.getDatasetMeta(0);
                                    const style = meta.controller.getStyle(i);

                                    return {
                                        text: label,
                                        fillStyle: style.backgroundColor,
                                        strokeStyle: style.borderColor,
                                        lineWidth: style.borderWidth,
                                        hidden: !chart.getDataVisibility(i),
                                        index: i
                                    };
                                });
                            }
                            return [];
                        }
                    },
                    onClick: function (e, legendItem, legend) {
                        const index = legendItem.index;
                        const chart = legend.chart;
                        chart.toggleDataVisibility(index);
                        chart.update();
                    }
                },
                tooltip: {
                    callbacks: {
                        label: function (context) {
                            const label = context.label || '';
                            const value = context.raw || 0;
                            const total = context.dataset.data.reduce((a, b) => a + b, 0);
                            const percentage = Math.round((value / total) * 100);

                            return `${label}: ${value} people (${percentage}%)`;
                        }
                    },
                    displayColors: true,
                    usePointStyle: true,
                    bodyFont: {
                        size: 14
                    },
                    padding: 12
                },
                title: {
                    display: true,
                    text: "Job Category",
                    font: {
                        size: 18,
                        weight: "bold",
                        family: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif"
                    },
                   
                    padding: {
                        top: 10,
                        bottom: 30
                    },
                    align: 'center'
                }
            },
            animation: {
                animateScale: true,
                animateRotate: true
            }
        }
    });
</script>
<!--WORKLOC CHART -->
<script>
    var jobCategoryData = @Html.Raw(Json.Serialize(Model.WorkSiteEmployees));
    var Total = @Model.WorkSiteEmployees.Select(el => el.zCount).Sum();
    var jobCategoryLabels = jobCategoryData.map(function (item) {
        return Math.round(((item.zCount / Total) * 100)*100)/100 + '% ' + item.zName;
    });

    var jobCategoryCounts = jobCategoryData.map(function (item) {
        return item.zCount;
    });

    var ctx = document.getElementById('workLocChart').getContext('2d');
    var jobCategoryChart = new Chart(ctx, {
        type: 'doughnut',
        data: {
            labels: jobCategoryLabels,
            datasets: [{
                data: jobCategoryCounts,
                backgroundColor: [
                    '#FF6B6B', '#FF8E72', '#FFB74D', '#FFD54F', '#DCE775',
                    '#A3E635', '#4CAF50', '#81C784', '#64B5F6', '#2196F3',
                    '#1565C0', '#9575CD', '#BA68C8', '#F06292', '#EC407A'
                ],
                borderColor: '#fff',
                borderWidth: 2,
                hoverOffset: 15,
                hoverBorderColor: '#333'
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: '50%', // This makes it a doughnut instead of pie
            plugins: {
                legend: {
                    position: 'bottom',
                    labels: {
                        padding: 10,
                        usePointStyle: true,
                        pointStyle: 'circle',
                        font: {
                            size: 12,
                            family: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif"
                        },
                        generateLabels: function (chart) {
                            const data = chart.data;
                            if (data.labels.length && data.datasets.length) {
                                return data.labels.map(function (label, i) {
                                    const meta = chart.getDatasetMeta(0);
                                    const style = meta.controller.getStyle(i);

                                    return {
                                        text: label,
                                        fillStyle: style.backgroundColor,
                                        strokeStyle: style.borderColor,
                                        lineWidth: style.borderWidth,
                                        hidden: !chart.getDataVisibility(i),
                                        index: i
                                    };
                                });
                            }
                            return [];
                        }
                    },
                    onClick: function (e, legendItem, legend) {
                        const index = legendItem.index;
                        const chart = legend.chart;
                        chart.toggleDataVisibility(index);
                        chart.update();
                    }
                },
                tooltip: {
                    callbacks: {
                        label: function (context) {
                            const label = context.label || '';
                            const value = context.raw || 0;
                            const total = context.dataset.data.reduce((a, b) => a + b, 0);
                            const percentage = Math.round((value / total) * 100);

                            return `${label}: ${value} people (${percentage}%)`;
                        }
                    },
                    displayColors: true,
                    usePointStyle: true,
                    bodyFont: {
                        size: 14
                    },
                    padding: 12
                },
                title: {
                    display: true,
                    text: "Work Loaction",
                    font: {
                        size: 18,
                        weight: "bold",
                        family: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif"
                    },
                  
                    padding: {
                        top: 10,
                        bottom: 30
                    },
                    align: 'center'
                }
            },
            animation: {
                animateScale: true,
                animateRotate: true
            }
        }
    });
</script>
<!--JOB CLASS CHART -->
<script>
    var jobClassData = @Html.Raw(Json.Serialize(Model.WorkSiteEmployees));
    var Total = @Model.WorkSiteEmployees.Select(el => el.zCount).Sum();

    var jobClassLabels = jobClassData.map(function (item) {
        return item.zName;
    });

    var jobClassCounts = jobClassData.map(function (item) {
        return item.zCount;
    });

    var ctx = document.getElementById('none').getContext('2d');
    var jobCategoryChart = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: jobClassLabels,
            datasets: [{
                label: 'Number of Employees',
                data: jobClassCounts,
                backgroundColor: [
                    '#FF6B6B', '#FF8E72', '#FFB74D', '#FFD54F', '#DCE775',
                    '#A3E635', '#4CAF50', '#81C784', '#64B5F6', '#2196F3',
                    '#1565C0', '#9575CD', '#BA68C8', '#F06292', '#EC407A'
                ],
                borderColor: '#fff',
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            scales: {
                y: {
                    beginAtZero: true,
                    ticks: {
                        precision: 0,
                        font: {
                            size: 14
                        }
                    },
                    title: {
                        display: true,
                        text: 'Number of Employees',
                        font: {
                            size: 16,
                            weight: 'bold'
                        }
                    }
                },
                x: {
                    ticks: {
                        font: {
                            size: 12
                        },
                        autoSkip: false,
                        maxRotation: 45,
                        minRotation: 45
                    },
                    title: {
                        display: true,
                        text: 'Job Classes',
                        font: {
                            size: 16,
                            weight: 'bold'
                        }
                    }
                }
            },
            plugins: {
                legend: {
                    display: false // Hide legend since we only have one dataset
                },
                tooltip: {
                    callbacks: {
                        label: function (context) {
                            const value = context.raw || 0;
                            const percentage = Math.round((value / Total) * 100);
                            return `${value} people (${percentage}%)`;
                        }
                    },
                    displayColors: false
                },
                title: {
                    display: true,
                    text: 'Job Classes Distribution',
                    font: {
                        size: 18,
                        weight: 'bold'
                    },
                    padding: {
                        top: 10,
                        bottom: 20
                    },
                    align: 'center'
                }
            },
            animation: {
                duration: 1000,
                easing: 'easeOutBounce'
            }
        }
    });
</script>
