@page
@model PIS2.Pages.JobPlacement.CreateModel

@{
    ViewData["Title"] = "Job Placements";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1 class="fw-bold">Job Placements</h1>

<h4 class="fw-bold">Assign New Job Placement</h4>
<hr />
<div class="">
    <form method="get" class="d-print-none">
        <div class="d-flex justify-content-between">

            <div class="d-flex">
                <input asp-for="employmentModel.givenID" id="givenID" class="form-control me-2" placeholder="Enter Given ID" name="givenID" />
                <button type="submit" class="btn btn-primary" id="goButton">Go</button>
            </div>

        </div>
    </form>
</div>
@if(Model.employmentModel != null && Model.employmentModel.employmentStatus == Models.mainStatus.Active)
{
    <div class="row">
        <h4 class="fw-bold"><u>@Model.employmentModel.personModel?.personFullName</u></h4>
        <div class="">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                
                <input asp-for="jobPlacementModel.employmentID" value="@Model.employmentModel.employmentID" type="hidden"/>
                <input asp-for="jobPlacementModel.jobPlacementStatus" value="@Models.mainStatus.Suspended" type="hidden"/>
      
                <div class="d-flex">
                    <div class="col-md-6 p-2">
                        <div class="form-group">
                            <label asp-for="company" class="control-label">Company</label>
                            <select asp-for="company" class ="form-control" asp-items="ViewBag.companyID" id="companyID">
                                <option value="">--Select Company--</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label asp-for="jobPlacementModel.departmentID" class="control-label">Department</label>
                            <select asp-for="jobPlacementModel.departmentID" class ="form-control" id="departmentID">
                                <option value="">--Select Department--</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label asp-for="jobPlacementModel.jobID" class="control-label">Job Title</label>
                            <select asp-for="jobPlacementModel.jobID" class ="form-control" asp-items="ViewBag.jobID" id="jobID"></select>
                        </div>
                        <div class="d-flex justify-content-between">
                            <div class="form-group">
                                <label class="control-label">Job Grade</label>
                                <select class="form-control" asp-items="ViewBag.jobGradeID" id="jobGradeID"></select>
                            </div>
                            <div class="form-group">
                                <label asp-for="jobPlacementModel.jobStepID" class="control-label">Job Step</label>
                                <select asp-for="jobPlacementModel.jobStepID" class="form-control" id="jobStepID"></select>
                            </div>
                            <div class="form-group">
                                <span class="text-danger">*</span>
                                <label asp-for="jobPlacementModel.jobPlacementSalary" class="control-label">Salary</label>
                                <input asp-for="jobPlacementModel.jobPlacementSalary" class="form-control" id="salary" />
                                <span asp-validation-for="jobPlacementModel.jobPlacementSalary" class="text-danger"></span>
                            </div>
                        </div>
   
                        
                    </div>
                    <div class="col-md-6 p-2">
                        <div class="form-group">
                            <label asp-for="jobPlacementModel.jobPlacementDate" class="control-label">Date</label>
                            <input asp-for="jobPlacementModel.jobPlacementDate" class="form-control" type="date" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                            <span asp-validation-for="jobPlacementModel.jobPlacementDate" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group">
                            <span class="text-danger">*</span>
                            <label asp-for="jobPlacementModel.jobPlacementReason" class="control-label">Reason</label>
                            <select asp-for="jobPlacementModel.jobPlacementReason" class="form-control">
                                <option value=""> -- Select Reason--</option>
                                <option value="New Hire"> New Hire</option>
                                <option value="Promotion"> Promotion</option>
                                <option value="Transfer"> Transfer</option>
                                <option value="Demotion"> Demotion</option>
                                <option value="Temporary Assignment"> Temporary Assignment</option>
                                <option value="Other"> Other</option>
                            </select>
                            <span asp-validation-for="jobPlacementModel.jobPlacementReason" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <span class="text-danger">*</span>
                            <label asp-for="jobPlacementModel.jobPlacementReference" class="control-label">Reference</label>
                            <input asp-for="jobPlacementModel.jobPlacementReference" class="form-control" />
                            <span asp-validation-for="jobPlacementModel.jobPlacementReference" class="text-danger"></span>
                        </div>
                    </div>
                </div>
                <div class="form-group d-flex justify-content-end">
                    <input type="submit" value="Save" class="btn btn-success m-2" />
                </div>
            </form>
        </div>
    </div>
}
else if (Model.employmentModel != null && Model.employmentModel.employmentStatus != Models.mainStatus.Active)
{
    <h5 class="fw-bold text-danger">Employee not active!</h5>
}
else if (Model.employmentModel == null || Model.employmentModel.employmentID == 0)
{
    <h5 class="fw-bold text-info">Enter Employee ID To Proceed!</h5>
}
<div>
    <a asp-page="/Employment/Edit" asp-route-id="@Model.employmentModel.employmentID">Back to Employee Page</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#companyID').change(function () {
                var companyID = $(this).val();
                $('#departmentID').empty().append('<option value="">-- Loading --</option>');

                if (companyID) {
                    $.getJSON('?handler=DepartmentsByCompany&companyID=' + companyID, function (data) {
                        $('#departmentID').empty().append('<option value="">-- Select Department --</option>');
                        $.each(data, function (i, dept) {
                            $('#departmentID').append($('<option>', {
                                value: dept.departmentID,
                                text: dept.departmentName
                            }));
                        });
                    });
                } else {
                    $('#departmentID').empty().append('<option value="">-- Select Department --</option>');
                }
            });
            $('#jobID').change(function () {
                var jobID = $(this).val();
                if (jobID) {
                    $.getJSON('?handler=JobGrade&jobID=' + jobID, function (data) {
                        $('#jobGradeID').val(data).trigger('change');

                    })
                }
            })
            
            function getJobSteps() {
                var jobGradeID = $('#jobGradeID').val();
                if (jobGradeID) {
                    $.getJSON('?handler=JobStep&jobGradeID=' + jobGradeID, function (data) {
                        var $jobStep = $('#jobStepID');
                        $jobStep.empty().append('');
                        $.each(data, function (i, js) {
                            $jobStep.append($('<option>', {
                                value: js.jobStepID,
                                text: js.jobStepNumber
                            }));
                        });
                        // Auto-select first option if available
                        if (data.length > 0) {
                            $jobStep.val(data[0].jobStepID).trigger('change'); // triggers getSalary
                        }
                    });

                    
                }
            }
            $('#jobGradeID').change(getJobSteps);
            if ($('#jobGradeID').val()) getJobSteps();

            function getSalary () {
                var jobStepID = $('#jobStepID').val();
                $('#salary').empty().append('0.00');
                
                if (jobStepID) {
                    $.getJSON('?handler=Salary&jobStepID=' + jobStepID, function (data) {
                        $('#salary').val(data)
                    })
                }
            }
            $('#jobStepID').change(getSalary);
            if ($('#jobStepID').val()) getSalary();
        });
       
    </script>
}
