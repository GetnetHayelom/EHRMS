@page
@model PIS2.Pages.Family.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.familyModel[0].personModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.familyModel[0].personID2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.familyModel[0].relation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.familyModel[0].modifiedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.familyModel[0].modifiedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.familyModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.personModel.personID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.personID2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.relation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.modifiedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.modifiedDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.familyID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.familyID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.familyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
